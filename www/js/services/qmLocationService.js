angular.module('starter')
    // Measurement Service
    .factory('quantimodoService', function($http, $q, $rootScope, $cordovaGeolocation, $ionicPlatform,
                                           localStorageService, quantimodoService) {
        
        // service methods
        var quantimodoService = {};



    });